# envy SSH mount

Mountpoint:

- `home`

Connection:

- Remote: `envy:/home/vlhnhp`
- Host alias: `envy`
- Type: SSHFS
- Systemd unit: `mnt-sshfs@envy-home.service`
- Mode: read/write

This README is outside the mounted `home` directory.
