# ac SSH mount

Mountpoint:

- `home`

Connection:

- Remote: `ac:/home/vlhnac`
- Host alias: `ac`
- Type: SSHFS
- Systemd unit: `mnt-sshfs@ac-home.service`
- Mode: read/write

This README is outside the mounted `home` directory.
